# Example environment configuration for Basic Financial Calculator
# Replace the value below with the secured BOT deposit rate endpoint for production use.
VITE_BOT_ENDPOINT="https://bot-deposit-rate.example.com/api/deposit_rate"
